# Step 1: Basic Router with Pattern Matching

This step creates a router that matches URLs to handlers with parameter extraction.

## Run this step

```bash
ruby step_demo.rb
```

## Concepts

- URL pattern compilation to regex
- Parameter extraction from URLs (`:id`, `:name`)
- Route matching by HTTP method
