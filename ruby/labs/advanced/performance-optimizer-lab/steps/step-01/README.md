# Step 1: Basic Benchmark Comparison

This step introduces Ruby's built-in `Benchmark` module.

## Run this step

```bash
ruby step_demo.rb
```

## Concepts

- `require 'benchmark'` for timing code
- `Benchmark.bm` for formatted comparisons
- Real time vs CPU time
