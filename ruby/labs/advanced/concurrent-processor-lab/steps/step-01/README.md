# Step 1: Basic Thread Pool

This step introduces the basic thread pool pattern using Ruby's `Thread` and `Queue`.

## Run this step

```bash
ruby step_demo.rb
```

## Concepts

- `Thread.new` for creating worker threads
- `Queue` for thread-safe job distribution
- Graceful shutdown with sentinel values
