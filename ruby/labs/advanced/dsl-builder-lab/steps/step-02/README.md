# Step 2: Add Nested Configuration

This step extends the DSL to support nested configuration blocks.

## Run this step

```bash
ruby step_demo.rb
```

## Concepts

- Block detection with `block_given?`
- Nested DSL classes for sub-configurations
- Recursive instance_eval for deep nesting
