# Step 7: Basic Query Builder with Method Chaining

This step introduces the Query Builder DSL using method chaining (fluent interface).

## Run this step

```bash
ruby step_demo.rb
```

## Concepts

- Method chaining by returning `self`
- Building SQL queries incrementally
- Supporting multiple condition formats (hash, string)
