<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Chat App - Sinatra Lab 4</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link rel="stylesheet" href="/css/style.css">
  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            primary: '#4F46E5',
            secondary: '#7C3AED',
          }
        }
      }
    }
  </script>
</head>
<body class="bg-gradient-to-br from-slate-50 to-slate-100 min-h-screen">
  <nav class="bg-white border-b border-slate-200 sticky top-0 z-50 shadow-sm">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between items-center h-16">
        <div class="flex items-center">
          <a href="/" class="flex items-center space-x-2 text-primary hover:text-secondary transition-colors">
            <i class="fas fa-comments text-2xl"></i>
            <span class="text-xl font-bold">ChatApp</span>
          </a>
        </div>
        <div class="flex items-center space-x-4">
          <% if logged_in? %>
            <div class="flex items-center space-x-3">
              <div class="flex items-center bg-primary/10 rounded-full px-4 py-2">
                <i class="fas fa-user-circle text-primary mr-2"></i>
                <span class="font-medium text-sm"><%= h(current_username) %></span>
              </div>
              <a href="/rooms" class="text-slate-700 hover:text-primary transition-colors font-medium">
                <i class="fas fa-door-open mr-1"></i> Rooms
              </a>
              <a href="/logout" class="bg-slate-100 hover:bg-slate-200 text-slate-700 px-4 py-2 rounded-lg transition-colors">
                <i class="fas fa-sign-out-alt mr-1"></i> Logout
              </a>
            </div>
          <% else %>
            <a href="/" class="bg-primary hover:bg-secondary text-white px-6 py-2 rounded-lg transition-colors font-medium">
              <i class="fas fa-sign-in-alt mr-2"></i>Join Chat
            </a>
          <% end %>
        </div>
      </div>
    </div>
  </nav>

  <main class="flex-1">
    <%= yield %>
  </main>

  <footer class="bg-white border-t border-slate-200 mt-auto">
    <div class="max-w-7xl mx-auto px-4 py-6 sm:px-6 lg:px-8">
      <p class="text-center text-slate-600 text-sm">
        Built with <strong class="text-primary">Sinatra + WebSockets</strong> |
        Lab 4: Real-time Chat |
        <a href="https://github.com/sinatra/sinatra" target="_blank" class="text-primary hover:text-secondary transition-colors">
          <i class="fab fa-github mr-1"></i>Learn More
        </a>
      </p>
    </div>
  </footer>
</body>
</html>
