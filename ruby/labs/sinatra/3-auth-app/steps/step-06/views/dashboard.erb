<div class="dashboard">
  <div class="page-header">
    <h1>Dashboard</h1>
    <p class="subtitle">Welcome, <%= h(current_user.name) %>!</p>
  </div>

  <div class="dashboard-grid">
    <div class="dashboard-section">
      <h2>Quick Actions</h2>
      <div class="action-list">
        <a href="/profile" class="action-item">
          <span class="action-icon">👤</span>
          <div class="action-content">
            <strong>View Profile</strong>
            <small>See your account details</small>
          </div>
          <span class="action-arrow">→</span>
        </a>
      </div>
    </div>

    <div class="dashboard-section">
      <h2>Account Info</h2>
      <div class="info-row">
        <span class="info-label">Email</span>
        <span class="info-value"><%= h(current_user.email) %></span>
      </div>
      <div class="info-row">
        <span class="info-label">Member Since</span>
        <span class="info-value"><%= current_user.created_at.strftime('%B %d, %Y') %></span>
      </div>
    </div>
  </div>
</div>
