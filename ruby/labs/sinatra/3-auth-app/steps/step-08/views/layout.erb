<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Auth App</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <nav class="navbar">
    <div class="container">
      <div class="navbar-brand">
        <a href="/">Auth App</a>
      </div>
      <div class="navbar-menu">
        <% if logged_in? %>
          <a href="/dashboard">Dashboard</a>
          <a href="/profile">Profile</a>
          <span class="navbar-user"><%= h(current_user.name) %></span>
          <a href="/logout" class="btn btn-secondary">Logout</a>
        <% else %>
          <a href="/login">Login</a>
          <a href="/register" class="btn btn-primary">Register</a>
        <% end %>
      </div>
    </div>
  </nav>

  <main class="container">
    <% if flash[:success] %>
      <div class="alert alert-success"><%= h(flash[:success]) %></div>
    <% end %>
    <% if flash[:error] %>
      <div class="alert alert-error"><%= h(flash[:error]) %></div>
    <% end %>
    <% if flash[:warning] %>
      <div class="alert alert-warning"><%= h(flash[:warning]) %></div>
    <% end %>

    <%= yield %>
  </main>

  <footer class="footer">
    <div class="container">
      <p>Built with <strong>Sinatra</strong> | Lab 3: Authentication & Sessions</p>
    </div>
  </footer>
</body>
</html>
