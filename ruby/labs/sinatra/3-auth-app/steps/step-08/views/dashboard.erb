<div class="dashboard">
  <div class="page-header">
    <h1>Dashboard</h1>
    <p class="subtitle">Welcome, <%= h(current_user.name) %>!</p>
  </div>

  <div class="stats-grid">
    <div class="stat-card">
      <span class="stat-icon">👤</span>
      <div class="stat-content">
        <span class="stat-label">Account Status</span>
        <span class="stat-value">Active</span>
      </div>
    </div>
    <div class="stat-card">
      <span class="stat-icon">📅</span>
      <div class="stat-content">
        <span class="stat-label">Member Since</span>
        <span class="stat-value"><%= current_user.created_at.strftime('%b %d, %Y') %></span>
      </div>
    </div>
    <% if current_user.last_login_at %>
    <div class="stat-card">
      <span class="stat-icon">🕐</span>
      <div class="stat-content">
        <span class="stat-label">Last Login</span>
        <span class="stat-value"><%= current_user.last_login_at.strftime('%b %d, %Y') %></span>
      </div>
    </div>
    <% end %>
  </div>

  <div class="dashboard-grid">
    <div class="dashboard-section">
      <h2>Quick Actions</h2>
      <div class="action-list">
        <a href="/profile" class="action-item">
          <span class="action-icon">👤</span>
          <div class="action-content">
            <strong>View Profile</strong>
            <small>See your account details</small>
          </div>
          <span class="action-arrow">→</span>
        </a>
        <a href="/profile/edit" class="action-item">
          <span class="action-icon">✏️</span>
          <div class="action-content">
            <strong>Edit Profile</strong>
            <small>Update your information</small>
          </div>
          <span class="action-arrow">→</span>
        </a>
      </div>
    </div>

    <div class="dashboard-section">
      <h2>Security Status</h2>
      <div class="security-status">
        <div class="security-item">
          <span class="security-icon">✓</span>
          <span>Password protected with BCrypt</span>
        </div>
        <div class="security-item">
          <span class="security-icon">✓</span>
          <span>CSRF protection enabled</span>
        </div>
        <div class="security-item">
          <span class="security-icon">✓</span>
          <span>Secure session management</span>
        </div>
      </div>
    </div>
  </div>
</div>
