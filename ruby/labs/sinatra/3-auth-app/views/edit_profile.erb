<div class="edit-profile-page">
  <div class="page-header">
    <h1>Edit Profile</h1>
    <a href="/profile" class="btn btn-secondary">← Back to Profile</a>
  </div>

  <div class="edit-grid">
    <div class="edit-main">
      <div class="form-section">
        <h2>Personal Information</h2>

        <% if current_user.errors.any? %>
          <div class="alert alert-error">
            <strong>Please fix the following errors:</strong>
            <ul>
              <% current_user.errors.full_messages.each do |message| %>
                <li><%= h(message) %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <form action="/profile/edit" method="post" class="edit-form">
          <%= csrf_tag %>

          <div class="form-group">
            <label for="name">Full Name</label>
            <input type="text" id="name" name="name" class="form-control"
                   value="<%= h(current_user.name) %>" required>
            <small class="form-text">This is how your name appears on your profile.</small>
          </div>

          <div class="form-group">
            <label for="email">Email Address</label>
            <input type="email" id="email" name="email" class="form-control"
                   value="<%= h(current_user.email) %>" required>
            <small class="form-text">Your email is used for login and notifications.</small>
          </div>

          <div class="form-actions">
            <button type="submit" class="btn btn-primary">
              💾 Save Changes
            </button>
            <a href="/profile" class="btn btn-secondary">Cancel</a>
          </div>
        </form>
      </div>

      <div class="form-section" id="change-password">
        <h2>Change Password</h2>

        <form action="/profile/password" method="post" class="edit-form">
          <%= csrf_tag %>

          <div class="form-group">
            <label for="current_password">Current Password</label>
            <input type="password" id="current_password" name="current_password"
                   class="form-control" required>
            <small class="form-text">Enter your current password to confirm it's you.</small>
          </div>

          <div class="form-group">
            <label for="new_password">New Password</label>
            <input type="password" id="new_password" name="new_password"
                   class="form-control" required>
            <small class="form-text">Must be at least 6 characters long.</small>
          </div>

          <div class="form-group">
            <label for="new_password_confirmation">Confirm New Password</label>
            <input type="password" id="new_password_confirmation"
                   name="new_password_confirmation" class="form-control" required>
            <small class="form-text">Re-enter your new password.</small>
          </div>

          <div class="form-actions">
            <button type="submit" class="btn btn-warning">
              🔐 Change Password
            </button>
          </div>
        </form>
      </div>
    </div>

    <div class="edit-sidebar">
      <div class="sidebar-section">
        <h3>Profile Tips</h3>
        <ul class="tips-list">
          <li>Use your real name so others can recognize you</li>
          <li>Keep your email up to date for important notifications</li>
          <li>Use a strong, unique password</li>
          <li>Consider enabling "remember me" on trusted devices</li>
        </ul>
      </div>

      <div class="sidebar-section">
        <h3>Security Best Practices</h3>
        <div class="security-checklist">
          <div class="checklist-item">
            <span class="check">✓</span>
            <span>Use a password manager</span>
          </div>
          <div class="checklist-item">
            <span class="check">✓</span>
            <span>Change password regularly</span>
          </div>
          <div class="checklist-item">
            <span class="check">✓</span>
            <span>Don't reuse passwords</span>
          </div>
          <div class="checklist-item">
            <span class="check">✓</span>
            <span>Logout on shared devices</span>
          </div>
        </div>
      </div>

      <div class="sidebar-section warning">
        <h3>⚠️ Important</h3>
        <p>
          Changes to your email or password will affect your ability to login.
          Make sure you remember your new credentials.
        </p>
      </div>
    </div>
  </div>
</div>
