# Step 3: User Model with Authentication

This step adds User model with BCrypt password hashing.

## What's Included
- User model with validations
- BCrypt password hashing
- Sample user seeded

## How to Run
```bash
ruby app.rb
```

## Test Endpoints
```bash
curl http://localhost:4567/api/v1/users
curl http://localhost:4567/api/v1/health
```
